# G-2 — Claim UI + adjuster scheduling — execution plan

Spec: `docs/superpowers/specs/2026-06-29-g2-claim-ui-design.md`. Worktree: `.claude/worktrees/claim-ui` (off origin/main @ 069ed89). One PR off `main`.

## Task 1 — `adjuster` appointment type + migration 0031  (mechanical · inline/haiku)
- Edit `packages/core/src/enums.ts`: `APPOINTMENT_TYPE = ["inspection","cm","crew","adjuster"] as const;`
- Generate migration: `pnpm db:generate` (from repo). If drizzle emits the `ALTER TYPE ... ADD VALUE` SQL, keep it; else hand-write `packages/db/drizzle/0031_*.sql` with `ALTER TYPE "appointment_type" ADD VALUE IF NOT EXISTS 'adjuster';` and add the `_journal.json` entry + snapshot.
- `git add packages/db/drizzle` (whole dir — commit meta).
- Verify: `pnpm --filter @savvy/core typecheck`.

## Task 2 — db: `bookAdjusterMeeting` + `getAdjusterAppointmentForJob` (TDD · sonnet)
- TEST FIRST: `packages/db/src/lifecycle/claim.test.ts` (or extend existing claim test). Use `.js` on relative imports. Assert:
  1. `bookAdjusterMeeting` creates an appointment with `type='adjuster'`, `status='scheduled'`, and flips `claim.status` to `'adjuster_scheduled'` (creating the claim row when none exists; updating when it does).
  2. `getAdjusterAppointmentForJob` returns the scheduled adjuster appt (not `cm`/`inspection`), and `null` when none.
  3. (cross-tenant) reading from another tenant returns null.
- IMPL in `packages/db/src/lifecycle/claim.ts`: import `bookAppointment` from `./appointments`; add both fns. Export from `packages/db/src/index.ts`.
- Verify: `pnpm --filter @savvy/db test claim` green; `pnpm --filter @savvy/db typecheck`.

## Task 3 — web: server actions `apps/web/src/lib/claim-actions.ts`  (sonnet)
- `"use server"`. `saveClaimAction(input)` + `bookAdjusterMeetingAction(input)` per spec. Dollar→cents helper (parse, round, null on empty); date-string→Date|null. Discriminated-union returns. `revalidatePath(`/jobs/${jobId}`)`.
- Catch `SlotTakenError` (import from `@savvy/db`) → `{ error:'slot_taken' }`.
- Verify: `pnpm --filter @savvy/web typecheck`.

## Task 4 — web: `ClaimPanel.tsx` client component  (sonnet)
- `apps/web/src/app/(app)/jobs/[id]/ClaimPanel.tsx`, `"use client"`. Props: `{ jobId, claim, adjusterAppointment, tasks }`. Mirror MaterialsPanel (useTransition, router.refresh, data-testid). Claim form + status select + dollar/date inputs + Save; adjuster sub-section (scheduled appt readout OR datetime-local + Book, handle `slot_taken`); read-only task list (title + StatusBadge).
- Verify: `pnpm --filter @savvy/web typecheck`.

## Task 5 — web: wire into `page.tsx`  (sonnet/inline)
- Import `getClaimForJob`, `getAdjusterAppointmentForJob` from `@savvy/db` + `ClaimPanel`. When `job.type==='insurance'`: top-level await both; filter fetched tasks to `phase==="Insurance Claim Management"`; render `<ClaimPanel>` in a `<Card>` near Materials. Non-insurance: render nothing new.
- Verify: `pnpm --filter @savvy/web typecheck` + `pnpm lint`.

## Task 6 — verify whole branch + final review (controller + opus)
- `pnpm typecheck` + `pnpm lint` + `pnpm --filter @savvy/db test` green.
- Playwright e2e for the claim panel IF a seeded insurance job exists (select by unique handle, never positionally); else document why skipped.
- Final whole-branch review on opus (UI + new db fns + migration RLS). Then PR off main, `gh pr checks <n> --watch` to green, squash-merge, clean up worktree + remote branch, update memory.
